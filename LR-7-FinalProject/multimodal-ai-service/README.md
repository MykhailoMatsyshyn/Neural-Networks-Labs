# Multimodal AI Service

Повнофункціональний мультимодальний AI сервіс з React фронтендом та FastAPI backend, який поєднує RAG, Agent Tools, генерацію зображень та аналіз зображень.

## Функціональність

- 💬 **Чат режим** - стандартна взаємодія з AI
- 🎨 **Генерація зображень** - створення зображень на основі текстового опису
- 🔍 **Аналіз зображень** - аналіз завантажених зображень
- 📚 **RAG (Retrieval-Augmented Generation)** - пошук та використання релевантних документів
- 🤖 **Agent Tools** - інтеграція з інструментами AI агента
- 💾 **Локальне зберігання** - автоматичне збереження потоків у localStorage

## Технології

### Frontend

- React 19
- Vite
- Tailwind CSS
- Lucide React (іконки)

### Backend

- FastAPI
- OpenAI API (LM Studio)
- ChromaDB (векторна база даних)
- Uvicorn

## Встановлення

### Frontend

```bash
cd frontend
npm install
```

### Backend

```bash
cd backend
pip install -r requirements.txt
```

## Запуск

### Frontend

```bash
cd frontend
npm run dev
```

### Backend

```bash
cd backend
python main.py
```

Або через uvicorn:

```bash
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

**Примітка:** Backend підтримує два режими:

- **Реальний OpenAI API** (за замовчуванням) - встановіть `OPENAI_API_KEY` в `.env` файлі або змінних оточення
- **LM Studio** (локальна модель) - встановіть `USE_LM_STUDIO=true` в `.env` та запустіть LM Studio на `http://localhost:1234`

## Збірка

```bash
cd frontend
npm run build
```

## Структура проекту

```
multimodal-ai-service/
├── frontend/                    # Frontend додаток (React)
│   ├── src/
│   │   ├── MultimodalAIService.jsx  # Основний компонент
│   │   ├── App.jsx                  # App компонент
│   │   ├── main.jsx                 # Точка входу
│   │   └── style.css                # Tailwind стилі
│   ├── public/                      # Статичні файли
│   ├── index.html                   # HTML шаблон
│   ├── vite.config.js               # Конфігурація Vite
│   ├── tailwind.config.js           # Конфігурація Tailwind
│   └── package.json                 # Залежності проекту
├── backend/                     # Backend сервер (FastAPI)
│   ├── main.py                      # Головний файл з API
│   ├── requirements.txt             # Python залежності
│   ├── README.md                    # Backend документація
│   └── chroma_db/                   # Векторна база даних (створюється автоматично)
├── models/                      # Моделі даних (поки пусто)
└── README.md                    # Документація
```

## Налаштування

У компоненті є панель налаштувань, де можна змінити:

- Модель AI (GPT-4o Mini, GPT-4o, Claude Sonnet)
- Temperature параметр
- Увімкнути/вимкнути RAG
- Увімкнути/вимкнути Agent Tools

## Примітки

- Backend використовує LM Studio для локального запуску LLM моделей
- Для роботи з зображеннями потрібно налаштувати LLaVA модель в LM Studio
- ChromaDB зберігає документи локально в папці `backend/chroma_db`
- Генерація зображень поки не реалізована (mock) - потрібна інтеграція з SDXL/FLUX

## Документація

- [Frontend README](frontend/README.md) - деталі про фронтенд
- [Backend README](backend/README.md) - деталі про backend та API endpoints
- [TESTING.md](TESTING.md) - інструкції для тестування всіх функцій

## Швидкий старт

1. **Запустіть LM Studio** на `http://localhost:1234` з завантаженою моделлю
2. **Запустіть Backend:**
   ```bash
   cd backend
   python main.py
   ```
3. **Запустіть Frontend:**
   ```bash
   cd frontend
   npm run dev
   ```
4. Відкрийте браузер на `http://localhost:5173` (або інший порт, який покаже Vite)

Frontend автоматично підключається до backend на `http://localhost:8000`
