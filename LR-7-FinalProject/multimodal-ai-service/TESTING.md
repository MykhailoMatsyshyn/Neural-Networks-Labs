# Інструкції для тестування

## Передумови

1. **Backend запущено** на `http://localhost:8000`
2. **Frontend запущено** (зазвичай на `http://localhost:5173`)
3. **LM Studio запущено** на `http://localhost:1234` з завантаженою моделлю

## Крок 1: Запуск сервісів

### Backend

```bash
cd backend
python main.py
```

Ви повинні побачити:

```
INFO:     Uvicorn running on http://0.0.0.0:8000
```

### Frontend

```bash
cd frontend
npm run dev
```

Ви повинні побачити:

```
  VITE v7.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
```

### LM Studio

1. Запустіть LM Studio
2. Завантажте модель (наприклад, LLaVA для vision або будь-яку іншу для чату)
3. Запустіть сервер на порту 1234

## Крок 2: Тестування функцій

### 1. Тест базового чату

1. Відкрийте frontend в браузері
2. Виберіть режим "💬 Chat"
3. Введіть повідомлення: "Hello, how are you?"
4. Натисніть Enter або кнопку Send
5. Перевірте, чи прийшла відповідь від AI

### 2. Тест Agent Tools

**Тест функції get_item_price:**

- Введіть: "What is the price of iPhone 15?"
- Очікуваний результат: Виклик інструменту `get_item_price` з результатом

**Тест функції book_meeting:**

- Введіть: "Schedule a meeting about project"
- Очікуваний результат: Виклик інструменту `book_meeting`

### 3. Тест RAG (якщо ChromaDB встановлено)

1. Спочатку завантажте документи:

```bash
curl -X POST "http://localhost:8000/upload_documents" \
  -F "files=@document1.txt" \
  -F "files=@document2.txt"
```

2. У чаті запитайте щось, що стосується завантажених документів
3. Перевірте, чи з'являються RAG tools у відповіді

### 4. Тест аналізу зображень

1. Виберіть режим "🔍 Analyze"
2. Натисніть кнопку Upload (📤)
3. Виберіть зображення
4. Введіть питання (або залиште порожнім для загального опису)
5. Натисніть Send
6. Перевірте, чи прийшов аналіз зображення

**Примітка:** Для цього потрібна LLaVA модель в LM Studio

### 5. Тест генерації зображень

1. Виберіть режим "🎨 Generate"
2. Введіть опис: "A beautiful sunset over mountains"
3. Натисніть Send
4. Перевірте, чи з'явилося згенероване зображення

**Примітка:** Зараз це mock функція, реальна генерація потребує додаткової інтеграції

## Крок 3: Перевірка помилок

### Якщо frontend не підключається до backend:

1. Перевірте, чи backend запущено:

   ```bash
   curl http://localhost:8000/docs
   ```

   Має відкритися Swagger документація

2. Перевірте CORS налаштування в backend (має бути `allow_origins=["*"]`)

3. Перевірте консоль браузера (F12) на наявність помилок

### Якщо AI не відповідає:

1. Перевірте, чи LM Studio запущено
2. Перевірте, чи модель завантажена
3. Перевірте логи backend на наявність помилок
4. Перевірте, чи правильний URL в налаштуваннях (`http://localhost:1234/v1`)

## Крок 4: Тестування через API напряму

Ви можете тестувати backend напряму через Swagger UI:

1. Відкрийте `http://localhost:8000/docs` в браузері
2. Знайдіть endpoint `/chat`
3. Натисніть "Try it out"
4. Введіть тестові дані:

```json
{
  "thread_id": "test123",
  "message": "Hello",
  "mode": "chat",
  "image_base64": null,
  "settings": {
    "model": "gpt-4o-mini",
    "temperature": 0.7,
    "enableRAG": true,
    "enableAgent": true
  }
}
```

5. Натисніть "Execute"
6. Перевірте відповідь

## Корисні команди

### Перевірка статусу backend

```bash
curl http://localhost:8000/docs
```

### Тестовий запит до chat endpoint

```bash
curl -X POST "http://localhost:8000/chat" \
  -H "Content-Type: application/json" \
  -d '{
    "thread_id": "test",
    "message": "Hello",
    "mode": "chat",
    "settings": {}
  }'
```

### Перевірка пошуку документів (RAG)

```bash
curl "http://localhost:8000/search_documents?query=test"
```

## Наступні кроки

Після успішного тестування базових функцій:

1. **Інтегрувати реальну генерацію зображень** (SDXL/FLUX через diffusers або ComfyUI)
2. **Додати більше Agent Tools** (email, calendar, database queries)
3. **Покращити RAG** (додати підтримку PDF, Word документів)
4. **Додати аутентифікацію** для production використання
5. **Оптимізувати продуктивність** (кешування, streaming responses)
